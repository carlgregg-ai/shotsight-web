let shooterAnswers={};let shooterStep=0;let diagnosticLessons=[];
const symptomOptions=[['behind','Behind','The miss is repeatedly behind the target.'],['ahead','In front','The miss is repeatedly in front of the target.'],['vertical','Above / below','The error is mainly vertical or over/under the target.'],['inconsistent','Erratic / timing changes','The miss or timing varies without a stable pattern.'],['lose','Lose the target','The target disappears from useful visual attention.'],['rush','Rush / stab / poke','The move or trigger decision feels rushed.'],['unsure','Not sure yet','The result is not repeatable enough to classify.']];
const diagnosticCompatibility={flat_long_crosser:['behind'],quartering_away:['behind'],low_fast_incomer_cutoff:['vertical'],chandelle_apex:['vertical','lose'],rising_teal_under_power:['vertical','inconsistent'],crossing_rabbit:['ahead','inconsistent'],driven_incoming:['behind','lose','rush'],pair_planning:['lose','rush'],rising_quartering_outgoing:['behind','lose'],fast_flat_quartering_away:['behind'],teal_near_apex:['rush','inconsistent']};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function buildDiagnosisUI(){const host=document.querySelector('#shooterDiagnosis');if(!host)return;host.innerHTML=`<div class="diagnosis-intro"><span class="diag-badge">PRESENTATION → RESULT → TEST → FIX → RETEST</span><h2>Work out what is actually going wrong</h2><p>ShotSight starts with the target, keeps competing causes visible, and only gives a correction where the certified Playbook has a diagnostic branch.</p><button class="primary big" id="startShooterDiagnosis">Start diagnosis <span>→</span></button></div>`;document.querySelector('#startShooterDiagnosis')?.addEventListener('click',openShooterDiagnosis)}
async function ensureDiagnosticLessons(){if(diagnosticLessons.length)return diagnosticLessons;const urls=['data/playbook-representative-v1.json?v=20260904-0408','data/playbook-expansion-stage8-v1.json?v=20260904-0432'];const data=await Promise.all(urls.map(async url=>{const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`${url}: HTTP ${r.status}`);return r.json()}));diagnosticLessons=data.flatMap(d=>Array.isArray(d.lessons)?d.lessons:[]);const ids=new Set();for(const l of diagnosticLessons){if(ids.has(l.id))throw new Error(`Duplicate diagnostic lesson id: ${l.id}`);ids.add(l.id)}return diagnosticLessons}
async function openShooterDiagnosis(){shooterAnswers={};shooterStep=0;const sheet=document.querySelector('#diagnosisSheet');sheet?.classList.add('active');sheet?.setAttribute('aria-hidden','false');const body=document.querySelector('#diagnosisBody');if(body)body.innerHTML='<span class="diag-badge">DIAGNOSTIC ENGINE</span><h2>Loading certified branches…</h2>';try{await ensureDiagnosticLessons();renderPresentationQuestion()}catch(err){if(body)body.innerHTML=`<span class="diag-badge">DIAGNOSTIC ENGINE</span><h2>Diagnosis unavailable</h2><p class="diagnosis-sub">ShotSight could not load the certified diagnostic data (${esc(err.message)}). No advice has been guessed.</p><div class="diagnosis-actions"><button class="primary" id="diagnosisRetry">Try again</button></div>`;document.querySelector('#diagnosisRetry')?.addEventListener('click',openShooterDiagnosis)}}
function closeShooterDiagnosis(){document.querySelector('#diagnosisSheet')?.classList.remove('active');document.querySelector('#diagnosisSheet')?.setAttribute('aria-hidden','true')}
function setProgress(text){const p=document.querySelector('#diagnosisProgress');if(p)p.textContent=text}
function optionButton(value,title,sub){return `<button class="diagnosis-option" data-answer="${esc(value)}"><span><strong>${esc(title)}</strong><small>${esc(sub)}</small></span><b>›</b></button>`}
function bindAnswers(fn){document.querySelectorAll('#diagnosisBody [data-answer]').forEach(btn=>btn.addEventListener('click',()=>fn(btn.dataset.answer)))}
function renderPresentationQuestion(){shooterStep=1;setProgress('1 / 4');const body=document.querySelector('#diagnosisBody');if(!body)return;const options=diagnosticLessons.map(l=>optionButton(l.id,l.name,l.understand));options.push(optionButton('unknown','I cannot classify it yet','Stop the engine from guessing. Use the Playbook to identify the presentation first.'));body.innerHTML=`<span class="diag-badge">1 · PRESENTATION</span><h2>Which target are you missing?</h2><p class="diagnosis-sub">Diagnosis starts with geometry. Similar-sounding targets do not automatically share the same cause or fix.</p><div class="diagnosis-options">${options.join('')}</div>`;bindAnswers(v=>{shooterAnswers.presentation=v;if(v==='unknown')return renderInsufficient('The target presentation is not yet classified.','Use the Playbook to identify the flight family/phase before asking ShotSight to prescribe a mechanism.');renderObservedResultQuestion()})}
function renderObservedResultQuestion(){shooterStep=2;setProgress('2 / 4');const lesson=diagnosticLessons.find(l=>l.id===shooterAnswers.presentation);const body=document.querySelector('#diagnosisBody');if(!body)return;body.innerHTML=`<span class="diag-badge">2 · OBSERVED RESULT</span><h2>What repeats on ${esc(lesson?.name||'this target')}?</h2><p class="diagnosis-sub">Choose what actually happens. ShotSight will not force every miss into the one certified branch for this presentation.</p><div class="diagnosis-options">${symptomOptions.map(o=>optionButton(o[0],o[1],o[2])).join('')}</div>`;bindAnswers(v=>{shooterAnswers.symptom=v;const allowed=diagnosticCompatibility[shooterAnswers.presentation]||[];if(v==='unsure'||!allowed.includes(v))return renderUnmatched();renderContextQuestion()})}
function renderContextQuestion(){shooterStep=3;setProgress('3 / 4');const lesson=diagnosticLessons.find(l=>l.id===shooterAnswers.presentation);const d=lesson?.diagnose?.[0];const body=document.querySelector('#diagnosisBody');if(!body||!d)return renderUnmatched();body.innerHTML=`<span class="diag-badge">3 · WHAT YOU SAW / FELT</span><h2>Before choosing a cause, can you describe the context clearly?</h2><p class="diagnosis-sub">The current certified branch is <strong>${esc(d.symptom)}</strong>. ${d.label==='SHOTSIGHT_HYPOTHESIS'?'Its mechanism/test layer is explicitly a ShotSight hypothesis. ':''}If that does not describe your repeatable miss, the engine should remain uncertain.</p><div class="diagnosis-options">${optionButton('clear','Yes — this is the same repeatable pattern','Continue to the branch’s discriminating test.')} ${optionButton('different','No — that description is not quite my miss','Do not inherit this branch’s correction.')} ${optionButton('unclear','I am not sure','Hold the diagnosis and gather a cleaner observation.')}</div>`;bindAnswers(v=>{shooterAnswers.context=v;if(v!=='clear')return renderInsufficient('The certified branch does not yet fit cleanly.','Repeat the presentation and record the miss more precisely before selecting a mechanism or correction.');renderDiscriminator()})}
function renderDiscriminator(){shooterStep=4;setProgress('4 / 4');const lesson=diagnosticLessons.find(l=>l.id===shooterAnswers.presentation);const d=lesson?.diagnose?.[0];const body=document.querySelector('#diagnosisBody');if(!body||!d)return renderUnmatched();body.innerHTML=`<span class="diag-badge">4 · DISCRIMINATING TEST</span><h2>${esc(d.question)}</h2><p class="diagnosis-sub">Use this question/test to distinguish the proposed mechanism from competing explanations. Do not answer from what you think “should” happen.</p><div class="diagnosis-options">${optionButton('supports','The test supports this branch','The observed change/pattern is consistent with the branch interpretation.')} ${optionButton('against','The test points away from this branch','Keep competing mechanisms alive; do not apply this fix as if proven.')} ${optionButton('untested','I have not tested it yet','Stop here and run the test before prescribing a correction.')}</div>`;bindAnswers(v=>{shooterAnswers.discriminator=v;if(v==='supports')renderSupportedResult();else if(v==='against')renderAlternativeResult();else renderInsufficient('The discriminating test has not been run.','Run the test described above, then return. ShotSight will not convert an untested answer into a diagnosis.')})}
function renderUnmatched(){const lesson=diagnosticLessons.find(l=>l.id===shooterAnswers.presentation);const d=lesson?.diagnose?.[0];const chosen=symptomOptions.find(o=>o[0]===shooterAnswers.symptom)?.[1]||'uncertain result';renderInsufficient(`No certified ${chosen.toLowerCase()} branch exists yet for ${lesson?.name||'this presentation'}.`,d?`The currently certified branch for this lesson is “${d.symptom}”. ShotSight will not borrow its fix for a different miss.`:'No certified diagnostic branch is available.')}
function renderInsufficient(title,reason){setProgress('UNCERTAIN');const body=document.querySelector('#diagnosisBody');if(!body)return;body.innerHTML=`<span class="diag-badge">UNCERTAINTY RETAINED</span><h2>${esc(title)}</h2><div class="diagnosis-confidence">Insufficient evidence</div><div class="diagnosis-explain"><span>WHY SHOTSIGHT STOPS HERE</span><p>${esc(reason)}</p></div><div class="diagnosis-fix"><span>NEXT BEST ACTION</span><strong>Improve the observation before prescribing the fix.</strong><p>Reclassify the presentation, repeat the target, or use the relevant Playbook lesson. A missing branch is not permission to invent one.</p></div><div class="diagnosis-actions"><button class="secondary-button" id="diagnosisAgain">Start again</button><button class="primary" id="diagnosisPlaybook">Open Playbook</button></div><p class="diagnosis-note">ShotSight is retaining uncertainty deliberately. Sense data can later add gun-motion evidence, but it should challenge as well as confirm the coaching hypothesis.</p>`;document.querySelector('#diagnosisAgain')?.addEventListener('click',renderPresentationQuestion);document.querySelector('#diagnosisPlaybook')?.addEventListener('click',openSelectedPlaybook)}
function renderSupportedResult(){const lesson=diagnosticLessons.find(l=>l.id===shooterAnswers.presentation),d=lesson?.diagnose?.[0];if(!d)return renderUnmatched();setProgress('RESULT');const body=document.querySelector('#diagnosisBody');body.innerHTML=`<span class="diag-badge">SUPPORTED COACHING HYPOTHESIS</span><h2>${esc(lesson.name)}</h2><div class="diagnosis-confidence">Branch supported — not proven</div><div class="diagnosis-tree-view"><div><span>PRESENTATION</span><strong>${esc(lesson.name)}</strong></div><i>↓</i><div><span>OBSERVED RESULT</span><strong>${esc(d.symptom)}</strong></div><i>↓</i><div><span>DISCRIMINATING TEST</span><strong>${esc(d.question)}</strong></div><i>↓</i><div class="active"><span>CANDIDATE MECHANISMS</span><strong>${esc((d.candidate_mechanisms||[]).join(' · '))}</strong></div></div><div class="diagnosis-explain"><span>INTERPRETATION</span><p>${esc(d.interpretation)}</p></div><div class="diagnosis-fix"><span>CORRECTION${d.label?` · ${esc(d.label)}`:''}</span><strong>${esc(d.fix)}</strong><p><strong>Retest:</strong> ${esc(d.retest)}</p></div><div class="diagnosis-actions"><button class="secondary-button" id="diagnosisAgain">Ask again</button><button class="primary" id="diagnosisPlaybook">Open lesson</button></div><p class="diagnosis-note">This remains a coaching hypothesis from a certified branch. It is not proof of where the shot went or a medical/vision/gun-fit diagnosis.</p>`;document.querySelector('#diagnosisAgain')?.addEventListener('click',renderPresentationQuestion);document.querySelector('#diagnosisPlaybook')?.addEventListener('click',openSelectedPlaybook)}
function renderAlternativeResult(){const lesson=diagnosticLessons.find(l=>l.id===shooterAnswers.presentation),d=lesson?.diagnose?.[0];setProgress('ALTERNATIVES');const body=document.querySelector('#diagnosisBody');body.innerHTML=`<span class="diag-badge">BRANCH NOT CONFIRMED</span><h2>Keep the alternatives open</h2><div class="diagnosis-confidence">Evidence points away</div><div class="diagnosis-explain"><span>CERTIFIED INTERPRETATION</span><p>${esc(d?.interpretation||'The selected test does not support this branch strongly enough to prescribe its correction.')}</p></div><div class="diagnosis-fix"><span>DO NOT PRESCRIBE YET</span><strong>The candidate mechanisms remain alternatives, not a diagnosis.</strong><p>${esc((d?.candidate_mechanisms||[]).join(' · '))}</p></div><div class="diagnosis-actions"><button class="secondary-button" id="diagnosisAgain">Start again</button><button class="primary" id="diagnosisPlaybook">Review lesson</button></div><p class="diagnosis-note">A failed discriminator is useful evidence. ShotSight should reduce confidence instead of forcing the original explanation.</p>`;document.querySelector('#diagnosisAgain')?.addEventListener('click',renderPresentationQuestion);document.querySelector('#diagnosisPlaybook')?.addEventListener('click',openSelectedPlaybook)}
function openSelectedPlaybook(){const id=shooterAnswers.presentation;closeShooterDiagnosis();window.shotSightGo?.('learnView');setTimeout(()=>window.ShotSightPlaybook?.openLesson?.(id,'diagnose'),180)}
document.querySelector('#diagnosisClose')?.addEventListener('click',closeShooterDiagnosis);buildDiagnosisUI();window.openShooterDiagnosis=openShooterDiagnosis;window.closeShooterDiagnosis=closeShooterDiagnosis;
